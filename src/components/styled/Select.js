import styled from 'styled-components';

export const Select = styled.select`
    position: relative;
    width: 100%;
    outline: none;
    padding: 10px 20px;
    height: 60px;
    border-radius: 10px;
    border: 2px solid #002EFF;
`;